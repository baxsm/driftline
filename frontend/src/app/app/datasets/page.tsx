import DatasetsScreen from "@/components/datasets/datasets-screen";

export const metadata = { title: "Datasets - driftline" };

export default function DatasetsPage() {
  return <DatasetsScreen />;
}
